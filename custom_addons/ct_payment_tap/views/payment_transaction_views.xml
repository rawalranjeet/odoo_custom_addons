<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form_inherit_payment_transaction" model="ir.ui.view">
        <field name="name">payment.transaction.form.inherit.payment.transaction</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_lang']" position="after">
                <button type="object" name="action_tap_3ds_verification" string="Complete 3DS Verification (TAP)" class="btn btn-primary" invisible="not (state == 'pending' and tap_3ds_auth_url)"/>
            </xpath>
        </field>
    </record>

</odoo>
