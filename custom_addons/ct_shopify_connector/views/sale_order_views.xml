<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="shopify_orders_view_action" model="ir.actions.act_window">
        <field name="name">Shopify Orders</field>
        <field name="res_model">sale.order</field>
        <field name="domain">[('shopify_order_id','!=',False)]</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="menu_shopify_orders"
        name="Shopify Orders"
        parent="menu_shopify_root"
        sequence="5"
        />

    <menuitem
        id="menu_shopify_orders_list"
        name="Orders"
        action="shopify_orders_view_action"
        parent="menu_shopify_orders"
        sequence="1"
        />

</odoo>
